#include<bits/stdc++.h>
typedef long long ll;
const int mod=1e9+7;
using namespace std;

void solve() { 
    ll n; cin >> n;
    vector<ll> nums(n);

    for (ll i = 0; i < n; i++) {
        cin >> nums[i];
    }

    
}

int main(){
    int t; cin >> t;
    //t=1;
    while(t--) solve();
    return 0;
}